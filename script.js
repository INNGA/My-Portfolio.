// script.js (Currently minimal for direct page navigation)

// You could add client-side form validation,
// dynamic content loading, or interactive elements here later.

document.addEventListener('DOMContentLoaded', () => {
    console.log('Portfolio loaded!');
    // Example: If you wanted to add a "scroll to top" button
    // window.addEventListener('scroll', () => {
    //     if (window.scrollY > 200) {
    //         // show button
    //     } else {
    //         // hide button
    //     }
    // });
});